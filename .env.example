# ─── Working Holiday Tax — Environment Variables ────────────────────────────
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to git.
#
# Currently this project has no server-side secrets (static site + WhatsApp).
# Add variables here as the project grows.
#
# Example (for future use):
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
# NEXT_PUBLIC_SITE_URL=https://workingholidaytax.com.au
