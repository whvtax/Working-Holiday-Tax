# דוח ביקורת מקיפה — Working Holiday Tax — 27 באוגוסט 2026

הופק בריצת לילה לפי `/full-audit`. חמישה סוכנים רצו במקביל, כל אחד קרא את הקוד
בפועל. סך הכל 45 route handlers נפתחו אחד־אחד, 244 קבצי TS/TSX נסרקו,
`npx jest` הורץ (23 חבילות / 498 בדיקות — כולן עוברות), `npm audit` הורץ,
וה־Policy Guard נבדק אמפירית מול 16 משפטי בדיקה.

---

## תקציר מנהלים

| תחום | CRITICAL | HIGH | MEDIUM | LOW |
|---|---:|---:|---:|---:|
| אבטחה | 0 | 3 | 6 | 10 |
| ביצועים | — | 4 | 8 | 8 |
| קוד מת | — | — | — | — |
| אמינות ושגיאות | — | 10 | 8 | 4 |
| ארכיטקטורה | — | 4 | 9 | 5 |
| **סה"כ** | **0** | **21** | **31** | **27** |

*(קוד מת אינו מדורג בחומרה — הוא מדווח לפי ודאות מחיקה. ראו סעיף 3.)*

**חמישה משפטים על המצב:**

1. **אין CRITICAL ואין ולו regression אחד.** כל תיקון אבטחה שתועד בדוחות
   הקודמים נבדק בקוד הנוכחי ונמצא עדיין במקומו. זו תוצאה טובה ולא מובנת מאליה.
2. **הפרויקט אינו חסום לפרודקשן מבחינת אבטחה** — אבל הוא כן חסום מבחינת
   *אמינות*. עשרה ממצאי HIGH באמינות מתארים מסלולים שבהם הודעה של לקוח, הוכחת
   תשלום או הגשת טופס נעלמות בשקט, בלי עקבות ובלי שאיש יידע.
3. **הבעיה החוזרת בכל התחומים היא אחת: שאילתות בלי `limit`.** ארבעה מקומות
   נשענים בשקט על תקרת 1000 השורות של PostgREST. הם לא שבורים היום — הם
   **יישברו בתאריך שאף אחד לא יקשר לשינוי קוד**, וברוב המקרים בלי הודעת שגיאה.
4. **הסכום הכספי היחיד שנשמר לצמיתות על לקוח נגזר ב־regex מטקסט חופשי**, והוא
   לוכד את המספר הלא נכון. זה הממצא היחיד בדוח עם משמעות רגולטורית.
5. **פיצול איכות ברור בין שני חצאי הפרויקט.** Will בנוי סביב interface, מייבא
   טיפוסים משותפים, ונבדק ב־17 חבילות טסטים יריבים. ה־CRM מגדיר את הטיפוסים
   שלו מחדש, ושכבת הנתונים שלו בת 852 השורות אינה מכוסה בטסט אחד.

---

## רשימת "עצור הכל" — לתקן לפני הדיפלוי הבא

ממוין לפי נזק בפועל, לא לפי תחום.

### דרגה 1 — אובדן שקט של נתוני לקוח

| # | ממצא | קובץ | תחום |
|---|---|---|---|
| B1 | **הודעת מדיה נכנסת נעלמת לתמיד.** הקלייםם נתפס, השגיאה נבלעת ב־`catch {}` ריק, Meta לא תשלח מחדש. מאז שהמסלול מקבל **הוכחות תשלום**, הנפגע הוא לקוח משלם. מסלול הטקסט הסמוך עושה את זה נכון — אותה תשתית, פשוט לא חוברה | `api/will/webhook/route.ts:501-527` | אמינות |
| B2 | **`fetchWaMedia` בלי timeout ובלי מגבלת גודל** על מסלול ציבורי. Meta מרשה 100MB. תלייה או OOM → ההודעה אבדה דרך B1 | `lib/will/channel.ts:291-320` | אמינות |
| B3 | **לקוח מסומן "שילם" בלי לקבל אישור ובלי קישור לטופס.** שלוש כתיבות ברצף בלי טרנזקציה; כשל בשלישית משאיר נתונים לא עקביים בלי עקבות. הרימיינדרים ימשיכו לנדנד לו למלא טופס שמעולם לא נשלח | `lib/will/service.ts:533-536` | אמינות |
| B4 | **`notifyFormReceived` מפסיק להתאים לקוחות מעל 1000.** הלקוח מילא שאלון, המערכת לא יודעת, והתזכורות ממשיכות לרדוף אותו. זו בדיוק הבעיה שהקובץ נכתב כדי לתקן | `lib/will/form-link.ts:40-49` | אמינות + ביצועים |

### דרגה 2 — הודעה שגויה ללקוח (compliance)

| # | ממצא | קובץ | תחום |
|---|---|---|---|
| B5 | **הודעה נשלחת ללקוח שביקש להפסיק.** `handlePaymentProofMedia` הוא המסלול היחיד שלא בודק `optedOut` לפני `deliverOut` | `lib/will/service.ts:484-536` | אמינות |
| B6 | **שליחה שהצליחה נראית ככושלת → המפעיל שולח שוב → הלקוח מקבל פעמיים.** כשל כתיבה *אחרי* ששידור הצליח מפיל את ה־handler ל־500 גולמי | `lib/will/channel.ts:252-259` | אמינות |
| B7 | **Follow-up שנשלח יכול להישלח שוב.** `catch` אחד מחזיר job מ־`DONE` ל־`FAILED`, ואז ספירת ה־cadence מתזמנת מחדש את אותה תבנית. אותו `catch` גם שקוף לגמרי — אין log, אין audit | `lib/will/scheduler.ts:339,359-365` | אמינות |

### דרגה 3 — נכונות כספית ורגולטורית

| # | ממצא | קובץ | תחום |
|---|---|---|---|
| B8 | **סכום ההחזר נגזר ב־regex משדה טקסט חופשי, ולוכד את המספר הלא נכון** — ה־`$` הראשון הוא סך החשבוניות, לא ההחזר. נכתב להיסטוריית המס הקבועה. ובנוסף: `type: 'owed'` בלתי מושג לפי הבנייה | `lib/db.ts:293-294,337,390` | ארכיטקטורה |
| B9 | **חישוב מדרגות המס משולש בשלושה קבצים, לא מיוצא, ואפס טסטים.** המספר שהלקוח רואה לפני שהוא פונה. עדכון ATO = שלוש עריכות בלי רשת ביטחון | `app/{,de/,ja/}calculator/CalculatorClient.tsx` | ארכיטקטורה |

### דרגה 4 — יציבות תשתית

| # | ממצא | קובץ | תחום |
|---|---|---|---|
| B10 | **`saveWaCreds` — שתי כתיבות נפרדות.** כשל באמצע משאיר טוקן חדש עם מספר ישן, ואז **כל** ההודעות הנכנסות נזרקות בשקט | `lib/will/channel.ts:63-69` | אמינות |
| B11 | **ל־tick אין `maxDuration` ול־`dueJobs` אין limit/order.** בוקר עמוס הורג את ה־invocation באמצע; `attempts` נשרפים בכל reclaim עד `FAILED` קבוע ו־cadence מת בשקט | `api/will/tick/route.ts` · `scheduler.ts:141-147` | אמינות |
| B12 | **`ensureDailyDigest` מכפיל jobs** ברגע ש־`will_jobs` עוברת 1000 שורות. הפונקציה הסמוכה עושה את אותה בדיקה נכון | `lib/will/scheduler.ts:100-103` | אמינות + ביצועים |
| B13 | **3 ממצאי HIGH באבטחה** — פירוט מלא בסעיף 1. שניים עסקיים שמעולם לא נסגרו, ואחד שנוצר מחלוף זמן ולא משינוי קוד | ראו סעיף 1 | אבטחה |

### דרגה 5 — ביצועים בערוץ ההכנסה

| # | ממצא | קובץ | תחום |
|---|---|---|---|
| B14 | **המאמר כולו נפרס מחדש בכל frame של גלילה**, ומאזין הגלילה נרשם מחדש בכל frame. ~420 עמודי מאמר — רוב תנועת ה־SEO | `blog/[slug]/GuideArticle.tsx:382-383,411,496` | ביצועים |
| B15 | **9 קבצי פונט ב־preload בכל עמוד ציבורי** כי DM Sans מבוקש כ־7 משקלים סטטיים. ב־`/ja` זה 9 פונטים לטיניים לרנדר קומץ מילים | `app/layout.tsx:19-35` | ביצועים |

---

## שלושה דברים שהדוח מצא ואי אפשר לתקן בקוד לבד

**1. חסימת ה־WHM נבנתה במלואה ומעולם לא חוברה.** חמישה רכיבים בארבע שכבות —
מסך הסבר ללקוח, מתג עקיפה ב־CRM, endpoint ציבורי, endpoint מאומת, וטבלה
ב־Supabase — כולם קיימים, מתועדים ועובדים. **אף טופס לא קורא להם.**
שתי הקריאות האפשריות הפוכות זו לזו: או שהחסימה נבנתה והושהתה בכוונה, או
שהחיווט האחרון נשכח — ואז כל לקוח WHM מאז מגיש החזר שלפי הכלל של העסק עצמו
אין בו כסף. שום דבר בריפו לא מכריע. **זה הפריט הראשון שצריך לשים מול ג'ו.**

**2. `isNdaCountry` קיים פעמיים, והגרסה החיה היא החלשה.** הוכח אמפירית: 16 מתוך
22 קלטים מתפצלים. הגרסה החיה מזהה **אחד** משמונה שמות מדינות ביפנית, בטופס
שמוצע ביפנית. לקוח יפני שמקליד `ドイツ` לא מזוהה כמקרה של אמנת מס — וזיהוי
כזה קיים בדיוק כדי לתפוס מי שזכאי לשיעורי תושב, שהם החזר גדול בהרבה.
הגרסה הטובה יושבת מאחורי חבילת טסטים עוברת ואינה בשימוש.

**3. `SecurityNotice` — רכיב שעבדו עליו עד WCAG 2.2.1 ואינו מרונדר בשום מקום.**
שני changelogs מתעדים עבודה מכוונת עליו באוגוסט. או שהוא הוצא בכוונה, או שהוא
נשמט בריפקטור והאתר איבד הודעה בשקט. **מחיקה כאן משמידה את הראיה.**

---

## 1. אבטחה (Security)

**0 CRITICAL · 3 HIGH · 6 MEDIUM · 10 LOW · אפס regressions.**

הסוכן פתח את כל 45 ה־route handlers, אימת כל תיקון שתועד בעבר מול הקוד הנוכחי,
והריץ את ה־Policy Guard אמפירית מול 16 משפטי בדיקה.

**Regression check — התוצאה המרכזית:** כל תיקון שנבדק **עדיין במקומו**:
נעילת אדמין per־IP (`crm-store.ts:128-132`), קשירת ה־OTP לשלב הסיסמה דרך
`crm_preauth` (`login/route.ts:64-68`), ולידציית IP מימין לשמאל
(`get-ip.ts:84-98`), ועיבוד לפני ה־ack ב־webhook עם החזרת 500 כדי לגרום ל־retry
(`webhook/route.ts:22,419-494,564`).

שני ממצאי ה־HIGH העסקיים (`WILL-PAY-01`, `WILL-AI-01`) מעולם לא נסגרו והתרחבו
מאז; ממצא ה־HIGH השלישי (Next.js) נוצר מחלוף זמן ולא משינוי קוד — כלומר גרסה
שהתיישנה, לא רגרסיה.

הדוח המלא של סוכן האבטחה, על כל 19 הממצאים והראיות, נשמר בנפרד ומצורף לחבילת
הלילה.

---

## 2. ביצועים (Performance)

**4 HIGH · 8 MEDIUM · 8 LOW.**

הסוכן מדד בפועל: גדלי CSS אחרי מיניפיקציה, גדלי קומפוננטות client אחרי gzip,
קורפוס הבלוג (141 מאמרים, גוף ממוצע 4,813 בתים), והתנהגות `next/font` מתוך
`node_modules` עצמו. `next build` לא רץ בסביבה הזו בגלל היעדר גישה ל־Google
Fonts — ובכל מקום שנדרש מספר מ־build אמיתי, הסוכן כתב **מה למדוד ואיך** במקום
לנחש. זו הסיבה שהדוח שווה קריאה.

**ארבעת ה־HIGH:**

1. **`GuideArticle` מפרס את המאמר כולו בכל frame** — `getHeadings`,
   `calcReadTime` ו־`parseBody` (פרסר Markdown ידני בן 159 שורות) רצים ~60 פעם
   בשנייה בזמן גלילה, ומאזין הגלילה נרשם מחדש בכל פעם. משפיע על ~420 עמודי
   מאמר, שהם רוב התנועה האורגנית.
2. **9 preloads של פונטים בכל עמוד ציבורי** — DM Sans מבוקש כ־7 משקלים
   סטטיים במקום כקובץ variable אחד, ו־Fraunces מכפיל את עצמו עם italic.
3. **Middleware רץ על כל בקשת עמוד ומגדיר headers שאף אחד לא קורא** — האתר
   שהוחזר בעמל רב למצב סטטי עדיין לא מקבל הגשה נקייה מה־CDN.
4. **כל שליחת טופס מושכת את כל `will_customers`** ומסננת ב־JavaScript, כשה־index
   הנכון כבר קיים ופשוט לא נקרא.

**מה שכבר עשוי נכון, ושווה לא לשבור:** 87 מתוך 90 עמודים הם Server Components;
הבלוג עובר prerender מלא; הסרת קריאות ה־`headers()` מה־root layout הייתה ההחלטה
הכי משמעותית בפרויקט מבחינת ביצועים ונעשתה נכון; הפולינג בשני הדשבורדים כבר
עבר אופטימיזציה אמיתית; וה־webhook מוקשח היטב.

**עשירייה מומלצת לפי יחס מאמץ/תועלת** מופיעה בדוח המלא, ובראשה `useMemo` על
שלוש הפונקציות ב־`GuideArticle` — 30 דקות עבודה, ותועלת עצומה.

---

## 3. קוד מת וכפילויות (Dead Code)

**3 קבצים יתומים · 16 exports מתים · 8 בלוקים מתים בתוך קבצים חיים · 65 מתוך
155 מפתחות תרגום לא בשימוש · 0 dependencies מיותרות · 7 אשכולות כפילות.**

**~765 שורות ניתנות למחיקה מיידית · ~1,545 שורות סה"כ אם מאחדים גם כפילויות.**

הדוח הזה בולט בכך שהוא **דחה** ממצאים לא פחות משהוא דיווח עליהם:

- טבלאות `wa_*` — **רדומות בהחלטה מתועדת**, לא מתות בטעות. מוחרגות מכל ספירה.
- עצי `de/`/`ja/` — תרגומים. שום איחוד עמודים לא מוצע.
- ארבעה API routes ללא קורא באפליקציה — **endpoints של HTTP וכלי מפעיל**, לא
  קוד מת. זו הטעות הקלאסית בקטגוריה הזו, והיא נבדקה ונדחתה במפורש.

**הקוד נקי טקסטואלית באופן חריג:** אפס `console.log`, אפס `debugger`, אפס קוד
מוער, אפס קוד בלתי־נגיש. הקוד המת כאן הוא **מבני** (דברים שנותקו מהעץ), לא
פסולת שנשארה בפונקציות.

**אשכול אחד גדול ואשכולות כפילות מרכזיים:** חסימת ה־WHM (ראו למעלה); שלושת
ה־intake routes שהם ~85% אותו קובץ; תשעה קובצי layout זהים בייטית;
`fetchWithTimeout` ×3; `FileUpload` ×4; טבלת מדרגות המס ×3; ו־`isHowToPost`
שנסחף בין title ל־slug.

**עבודת הערב נבדקה ונמצאה נקייה:** ל־`crm-design.css` אפס מחלקות לא בשימוש,
והוא חולק עם `will-scoped.css` בדיוק סלקטור אחד — `.will-scope`, שורש הscoping,
וזה נכון.

---

## 4. אמינות וטיפול בשגיאות (Error Handling & Resilience)

**10 HIGH · 8 MEDIUM · 4 LOW.** התחום עם הכי הרבה ממצאים חמורים, ובצדק.

כל ממצאי ה־HIGH מפורטים ברשימת ה"עצור הכל" למעלה (B1–B7, B10–B12), פרט לשניים:

- **`ensureSeeded` מרעיל את עצמו** — כישלון אחד משאיר promise דחוי לנצח,
  ומשבית את כל ה־templates על ה־instance עד ה־deploy הבא. חסרה שורת `.finally`
  אחת.
- **`setState` — שומר הבו־זמניות נכשל בשקט** אבל ההיסטוריה כבר נכתבה. ההגנה
  מפני lost־update עובדת מצוין; הבעיה היא שהכישלון בלתי נראה והקורא ממשיך
  כאילו המעבר קרה.

### חמש נקודות הכשל היחיד המסוכנות ביותר

1. **הקלייםם ב־`will_processed_messages` כתופס־הכול על מסלול המדיה.** ממיר כל
   תקלה חולפת לאובדן קבוע ובלתי נראה, ומוחק את העקבות.
2. **`channel.ts` בלי timeouts** — ארבע קריאות רשת בערוץ היחיד אל הלקוח ומהלקוח,
   בזמן ש־`claude.ts` מקפיד על timeout בכל קריאה.
3. **הסקדולר כתהליך יחיד, סדרתי, חסר תקציב זמן.**
4. **שאילתות לא מוגבלות שנשענות בשקט על תקרת 1000 השורות** — הערמומית מכולן,
   כי היא לא נכשלת אלא מחזירה תשובה חלקית שנראית שלמה.
5. **אין גבול טרנזקציוני סביב רצפי כתיבות רב־שלביים.**

### מה שמטופל היטב — ואסור לשבור

`claude.ts` הוא הקובץ הכי עמיד בפרויקט (timeout על כל קריאה, retry עם jitter רק
על 429/5xx, אימות פלט קפדני, כשל הופך ל־task אנושי); `resolveAiMode` נכשל closed
על המתג המסוכן ביותר; `bumpCounter` אטומי אמיתי ומהווה את ההגנה על ההוצאה
בתשלום; מסלול הטקסט ב־webhook מתוכנן לעמידות מלאה; **התחברות CRM נכשלת closed**
בזמן שתעבורה נכשלת open — בדיוק ההבחנה הנכונה; ו־`store-file.ts` עושה כתיבה
אטומית אמיתית עם שרשור ושמירת עותק פגום.

---

## 5. ארכיטקטורה ואיכות קוד (Architecture & Quality)

**ציון בריאות: 6.5 / 10** — 4 HIGH · 9 MEDIUM · 5 LOW.

**הנימוק בשורה אחת:** משמעת הטיפוסים כאן חזקה מהממוצע, אבל היא לא מיושמת
באופן אחיד — וההבדל בין שני חצאי הפרויקט הוא כמעט מדויק.

**מה שמושך למעלה:** `strict: true` אמיתי ו־`tsc --noEmit` נקי על 281 קבצים;
שערי בנייה סגורים (`ignoreBuildErrors: false`); **חמישה `any` אמיתיים בכל
הפרויקט ואפס `@ts-ignore`**; Will בנוי סביב `interface Store` עם שתי מימושים
ומייבא טיפוסים משותפים ל־UI; אין בלבול Server Actions/routes.

**מה שמושך למטה:** ה־CRM לא נהנה מאף אחת מהתכונות האלה — הוא מגדיר מחדש את
הטיפוסים שלו (ויש drift אמיתי שמגיע עד המסך), מרכז 2,874 שורות ו־72 `useState`
בקומפוננטה אחת, ושכבת הנתונים שלו בת 852 השורות אינה מכוסה בטסט אחד.

**הגבול CRM/Will — התשובה לשאלה שנשאלה:** התפר **מוצדק, והחוליה היחידה שכן
קיימת היא חוב.** ברמת האפליקציה ההפרדה נקייה לחלוטין. אבל קיימת אינטגרציה
אחת — `will_on_form_received()`, פונקציית plpgsql שרצה כ־trigger על `crm_tasks`
ומחפשת התאמת טלפון — והיא **בלתי נראית ל־TypeScript לגמרי**. מי שיקרא את
`src/lib/` יסיק שהמערכות מנותקות, וזה יהיה שגוי. ולוגיקת נרמול הטלפון משוכפלת
בשתי שפות (TS ו־SQL) בלי טסט שמאמת שקילות.

**ממצא נוסף שראוי לתשומת לב:** `jest.config.js` מריץ את כל 498 הבדיקות עם
`strict: false` ו־`diagnostics: false` — כלומר קבצי הטסט עצמם אינם עוברים שום
בדיקת טיפוסים, וקוד המקור מתקמפל תחת סמנטיקה שונה מזו של ה־build.

**עובדות מס קשיחות בתשעה קבצים בשלוש שפות** — שיעור ה־cents־per־km של ה־ATO
מופיע כ־`"88c per km"`, `"88 Cent pro km"` ו־`"1kmあたり88セント"`. עדכון שנתי
דורש תשע עריכות, אחת מהן בשפה שהמפתח כנראה לא קורא. `src/lib/constants.ts`
מכיל את פרטי הסוכנות אבל **אפס נתוני מס**.

---

## תוכנית פעולה מומלצת

ממוינת כך שחומרה גבוהה + מאמץ נמוך מופיעים ראשונים.

| # | ממצא | תחום | חומרה | מאמץ | קובץ |
|---|---|---|---|---|---|
| 1 | הוספת `optedOut` לתנאי הכניסה של הוכחת תשלום | אמינות | HIGH | **S** | `will/service.ts:491` |
| 2 | `.finally(() => this.seedingPromise = null)` ב־`ensureSeeded` | אמינות | MEDIUM | **S** | `store-supabase.ts:173` |
| 3 | `export const maxDuration = 60` ל־tick ולשלושת ה־intake routes | אמינות | HIGH | **S** | `tick/route.ts` ועוד 3 |
| 4 | `hasScheduledJobOfKind('DAILY_DIGEST')` במקום `listJobs()` | אמינות | HIGH | **S** | `scheduler.ts:100-103` |
| 5 | `.limit()` + `.order()` ל־`dueJobs` ולשאילתות הלא־מוגבלות | אמינות | HIGH | **S** | `store-supabase.ts` |
| 6 | `AbortSignal.timeout` + תקרת גודל ל־`fetchWaMedia` | אמינות | HIGH | **S** | `channel.ts:291-320` |
| 7 | `useMemo` על `getHeadings`/`calcReadTime`/`parseBody` | ביצועים | HIGH | **S** | `GuideArticle.tsx:382` |
| 8 | הסרת `weight` מ־DM Sans, `italic` מ־Fraunces | ביצועים | HIGH | **S** | `layout.tsx:19-35` |
| 9 | מחיקת 8 בלוקים מתים + 16 exports מתים (~765 שורות) | קוד מת | — | **S** | ראו סעיף 3 |
| 10 | הצלת מסלול המדיה: dead-letter + task במקום `catch {}` | אמינות | HIGH | **M** | `webhook/route.ts:501` |
| 11 | כתיבה שנכשלת אחרי שידור שהצליח לא תיראה ככשל שליחה | אמינות | HIGH | **M** | `channel.ts:252-259` |
| 12 | `saveWaCreds` כ־upsert אטומי יחיד | אמינות | HIGH | **M** | `channel.ts:63-69` |
| 13 | `catch` של הסקדולר: audit + לא להחזיר `DONE` ל־`FAILED` | אמינות | HIGH | **M** | `scheduler.ts:359-365` |
| 14 | `findCustomersByPhoneTail` עם עמודה גנרטיבית + index | אמינות | HIGH | **M** | `form-link.ts:44` |
| 15 | פיצוי כ־task כשאישור התשלום לא נשלח | אמינות | HIGH | **M** | `service.ts:533-536` |
| 16 | `src/lib/tax-calc.ts` + טסט מדרגות | ארכיטקטורה | HIGH | **M** | 3× `CalculatorClient` |
| 17 | `src/lib/crm-types.ts` — מקור אמת אחד | ארכיטקטורה | HIGH | **M** | `db.ts` + 3 צרכנים |
| 18 | מחיקת ה־middleware / צמצום ה־matcher | ביצועים | HIGH | **M** | `middleware.ts` |
| 19 | עמודת `refund_amount_cents` במקום ה־regex | ארכיטקטורה | HIGH | **L** | `db.ts:293-294` |
| 20 | `Footer` כ־Server Component, פיצול `Nav` | ביצועים | MEDIUM | **L** | `Nav.tsx`, `Footer.tsx` |
| 21 | פיצול `DashboardClient` לפי `View` | ארכיטקטורה | HIGH | **L** | `DashboardClient.tsx` |
| 22 | `supabase gen types` → `getSupabase<Database>()` | ארכיטקטורה | MEDIUM | **L** | `supabase.ts:19` |

**שורות 1–9 הן כולן S** — ורובן ניתנות ליישום בלי שיקול דעת עסקי. זו קבוצת
העבודה של הלילה.

---

## הערה על הריצה

חמשת הסוכנים רצו במלואם והחזירו פלט מלא. אף ריצה לא נכשלה ואף אחת לא הוחזרה
חלקית. כל ממצא בדוח הזה מקושר לקובץ ושורה כפי שדווח ע"י הסוכן המקורי.

הפלט המלא של כל סוכן — כולל כל הראיות, קטעי הקוד והתיקונים המוצעים במלואם —
נשמר בתמליל הריצה. הדוח הזה הוא שכבת האיחוד והדירוג מעליו.
