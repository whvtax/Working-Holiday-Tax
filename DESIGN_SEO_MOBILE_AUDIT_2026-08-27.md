# דוח ביקורת עיצוב, SEO ומובייל — 27 באוגוסט 2026

חמישה סוכנים רצו במקביל לפי `/design-seo-mobile-audit`. שניים מהם — צפיפות
מובייל וזרימה — **רינדרו את האתר בפועל** ב־Chromium ב־390×844 ומדדו מה־DOM,
ולא הסיקו מהקוד. כל ממצא כאן מקושר לקובץ ולשורה.

---

## תקציר מנהלים

| תחום | HIGH | MEDIUM | LOW |
|---|---:|---:|---:|
| SEO | 6 | 7 | 3 |
| עיצוב ואחידות | 4 | 9 | 6 |
| צפיפות מובייל | 3 | 5 | 3 |
| זרימה ואינטראקציה | 6 | 9 | 5 |
| קיצור טקסט UI | 4 | 7 | 4 |
| **סה"כ** | **23** | **37** | **21** |

**חמישה משפטים:**

1. **אין ולו קישור HTML אחד בין שלושת עצי השפה.** מחליף השפה מרנדר את
   הקישורים רק אחרי לחיצה, ולכן 334 העמודים בגרמנית וביפנית — כולל 282
   המדריכים המתורגמים — מתגלים דרך ה־sitemap בלבד. hreflang הוא רמז, לא קישור:
   הוא לא מעביר סמכות.
2. **המסלול מהטופס להגשה שבור בשלוש נקודות שכל אחת מהן לבדה מפילה המרות** —
   כשל ולידציה שזורק את הכפתור והשגיאה מחוץ למסך, נגיעה אחת בלוגו שמוחקת טופס
   מלא, ו־30–60 שניות שקט מוחלט בזמן ההעלאה.
3. **הכותרות ביפנית מתנגשות פיזית.** יחס `line-height` של 1.14–1.18 מתאים
   לטקסט לטיני; בגליפים יפניים, שממלאים את כל תיבת ה־em, השורות נוגעות. ההערה
   בקוד אומרת "לצמצם קצת, גליפים יפניים צפופים ויזואלית" — וזה בדיוק הפוך.
4. **הגרמנית והיפנית נושאות ניסוחים שהאנגלית כבר גזמה.** הגזימה בוצעה בשפה
   אחת, ולכן כל שיפור קריאוּת נשאר חלקי — למרות ששלוש השפות מתורגמות במלואן.
5. **חצי מהאתר בנוי היטב, וזה מה שאיפשר את ההשוואה** — הריתמוס האנכי זהה
   בשלוש השפות, `MobileCta` עומד בכל דרישות הנגישות של Apple, ו־141 המדריכים
   מתורגמים במלואם. הממצאים הם החריגים, לא הכלל.

---

## רשימת "עצור הכל"

### SEO — גילוי ואינדוקס

| # | ממצא | קובץ |
|---|---|---|
| S1 | **אפס קישורי HTML בין השפות.** `{open && ...}` — הקישורים לא קיימים ב־DOM עד לחיצה | `LanguageSwitcher.tsx:136` |
| S2 | **`<html lang="en-AU">` בכל 502 העמודים.** ה־lang האמיתי נקבע ב־JS. סורקי AI שלא מריצים JS רואים אתר יפני שמצהיר שהוא אנגלי | `layout.tsx:338` |
| S3 | **`dateModified == datePublished` ב־423 עמודי מדריכים**, וה־`lastmod` של de/ja נלקח מתאריך הפרסום האנגלי. הקבוע `CORPUS_REVISED` **קיים בשלושת הקבצים ואינו בשימוש** — 282 תרגומים מאוגוסט 2026 מוצהרים כ"לא השתנו מאז יולי 2024" | `blog/[slug]/page.tsx:12` · `sitemap.ts:131,140` |
| S4 | **פיצול ישות:** אותה ישות מוצהרת תחת `#business` ותחת `#organization` לפי שפה, ועוד צומת רביעי ללא `@id` כלל. ה־`aggregateRating` תלוי על צומת שהטיפוס שלו בלתי־פתיר | `layout.tsx:166` · `page.tsx:225,252` · `de/page.tsx:257` |
| S5 | **`@id` תלוי באוויר:** `#supervising-agent` מופנה מ־423 עמודים ולא מוגדר בשום מקום. זה סיגנל ה־E‑E‑A‑T היחיד שמבדיל את המדריכים, והוא לא נקרא | `blog/[slug]/page.tsx:489` |
| S6 | **מרכז הבלוג מקשר ל־9 מדריכים מתוך 141 ולאפס עמודי קטגוריה** — מסנני הקטגוריה הם `<button>`, לא `<Link>`. 396 עמודים בלי נתיב זחילה מהניווט | `BlogClient.tsx:63,394` |

### מובייל — המסלול להמרה

| # | ממצא | קובץ |
|---|---|---|
| M1 | **כשל ולידציה זורק את הכפתור, את הבאנר ואת השדה השגוי מחוץ למסך.** נמדד: הכפתור זז 439/495/590px, הבאנר לא נראה במלואו אף פעם, השדה השגוי ב־y −142 עד −2100. אין גלילה, אין focus, אין `aria-live` | 4 הטפסים |
| M2 | **שלב 2 גולל אל ראש הדף בזמן שהשגיאה 653px מתחת לקיפול** — המשתמש משוכנע שהכל תקין | `tax-form/FormClient.tsx:368` |
| M3 | **אין הגנה על טופס שלא נשמר.** נמדד: הקלדת 3 שדות → נגיעה בלוגו → back = הכל ריק. ה־snapshot נכתב רק במעבר לשלב 3 | `Nav.tsx:19` · `FormClient.tsx:388` |
| M4 | **שתי העלאות קבצים עם "Submitting…" סטטי** — בלי spinner, בלי אחוזים, בלי timeout. ואם ה־POST נכשל, הקבצים כבר עלו וניסיון חוזר מעלה מחדש | `submit-tax-form.ts:115-133` |
| M5 | **שורת ההצהרה 36px, ו־23% ממנה שני לינקים ל־target חדש** בתוך אותו `<label>`. נגיעה בטקסט כדי לסמן = נזרקת למסמך משפטי | `tax-form/FormClient.tsx:725-739` |
| M6 | **`onKeyDown` עושה `preventDefault` על `key:'Unidentified'`** — מה שמקלדות Android שולחות — בשלושת השדות הקריטיים. והמסנן ממילא לא עובד: הדבקה עוקפת אותו | `FormClient.tsx:561,566,585` |
| M7 | **כותרות יפניות מתנגשות פיזית.** יחס 1.14–1.18. ההערה בקוד מנמקת הפוך | `globals.css:265-274,2321` |
| M8 | **כפתור "חזרה למעלה" חותך חור ב־CTA הענבר.** נמדד: חפיפה 29×44px, בכל שפה, בכל עמוד עם CtaBand | `ScrollToTop.tsx:33` |
| M9 | **כפתור התוכן בבלוג יושב על עמודת הטקסט** — 48×48px של פרוזה מוסתרים ב־423 עמודים | `MobileTOC.tsx:141` |

### עיצוב

| # | ממצא | קובץ |
|---|---|---|
| D1 | **סקאלת הטיפוגרפיה הרב־לשונית מבוטלת ב־`!important`** בדיוק בשני ה־viewport שנושאים את התנועה. 82 גדלי פונט שהוקלדו ביד חיים רק ברצועת 640–1023px | `globals.css:265-275,305-310` |
| D2 | **הבלוק היפני נכתב נכון ולא נכנס לתוקף על אף כותרת** — 93 הצהרות `letterSpacing` שליליות inline ב־20 קבצי JA מנצחות אותו. tracking שלילי על קאנה הוא שגוי | `globals.css:3111` · `src/app/ja/**` |
| D3 | **`white-space: nowrap` מוריד את ה־H1 הגרמני ל־12px** בעמוד החשוב באתר — קטן מהתוויות שמתחתיו | `tax-form/FormClient.tsx:797` |
| D4 | **מעבר reduced-motion לא מכסה את המחלקה שבשימוש בפועל.** ההגנה מכסה `hero-animate-delay2`; הדפים משתמשים ב־`hero-animate-delay-2` **עם מקף**. 12 דפים בשלוש שפות מריצים אנימציה למי שביקש שלא | `globals.css:2527,2554` |

---

## מה כבר נעשה נכון

הביקורת הזו הייתה אפשרית **רק** משום שחצי מהאתר בנוי היטב, וההשוואה היא מה
שחשף את החריגים:

- **hreflang: 100% כיסוי, הדדיות מלאה, self-reference ו־`x-default`** בכל 502
  העמודים. אפס קישורים פנימיים שבורים מתוך 1,515 שנבדקו. אפס דליפה בין־לשונית.
- **אפס כותרות כפולות, אפס תיאורים כפולים, אפס `<title>` מעל 60 תווים** בשלוש
  השפות.
- **הריתמוס האנכי זהה בדיוק בשלוש השפות** — `py-16` ×102, `py-12` ×90 וכן הלאה.
  זו סקאלה אמיתית ולא ריווח אקראי.
- **`MobileCta`** — היסטרזיס 420/360, מודעות לכיוון גלילה, `prefers-reduced-motion`
  + `prefers-reduced-transparency` + `prefers-contrast`, ורטט ברגע ההתחייבות
  היחיד. הרכיב הטוב באתר.
- **`MobileTOC`** — focus trap, Escape, `inert`, שורות 44px, ומחיקה מודעת של
  drag handle כי "affordance הוא הבטחה שהרכיב לא יכול לקיים".
- **`type`/`inputMode`/`autoComplete` כמעט מושלמים** בכל ארבעת הטפסים, ו־
  `font-size: 16px !important` מתחת ל־640px שמונע auto-zoom ב־iOS.
- **אפס overflow אופקי** ב־30+ צירופי עמוד/שפה שנבדקו.
- **`tailwind.config.js` הוא מסמך עיצוב** — יחס ניגודיות מדוד ליד כל ערך, והסבר
  למה צבע מסוים הוסר לגמרי.

---

## הקווים האדומים — הבקרה עבדה

שלושת הכללים נאכפו בכל חמשת הסוכנים, ו**כל אחד מהם החזיר המלצות שנפסלו**. זו
הראיה שהסינון פעל ולא רק הוצהר:

| הקו | מה נפסל |
|---|---|
| **1 — ניסוח חותמת סוכן המס** | קיצור שורת המשנה הגרמנית ב־CTA הנייד (הפתרון הישיר ביותר לגובה 96px); הסרת שורת האשראי מהפוטר כדי לקצר אותו; קיצור ההודעה מתחת לכפתור השליחה כדי לפנות מקום לסטטוס העלאה; שינוי ה־`alt` של חותמת ה־TPB |
| **2 — "העסק הוא סוכן מס רשום"** | כל ניסוח בכיוון "your registered tax agent" / "unser Steuerberater". נשקל `registeredAgentNo` כתווית קצרה — ונפסל |
| **3 — תוכן הסבר מס** | קיצור פסקאות ההסבר הגרמניות בנות 9 השורות; הסרת בלוק "Quick Answer"; קיצור `quizIntro` שנושא את סף ה־$18,200; קיצור גופי ה־`GuideCta` |

**במקום כל אחד מאלה הוצע פתרון עיצובי בלבד** — `text-wrap: balance`, גודל פונט
לשפה אחת, ריווח, chunking — שמשאיר כל מילה במקומה.

**ממצא compliance שדווח ולא טופל:** ה־`alt` של חותמת ה־TPB
(`Nav.tsx:456`, `Footer.tsx:193`) הוא `"Registered Tax Agent 26233096"` — בהקשר
של עמוד שהישות שלו היא Working Holiday Tax, זה נקרא כטענה על מעמד העסק, בניגוד
לניסוח הנכון ב־`client-agreement/page.tsx:181`. **לא הוצע ניסוח חלופי** — זה
מחוץ לתחום של הביקורת. **מסומן לבדיקה משפטית.**

---

## תוכנית פעולה

| # | ממצא | תחום | חומרה | מאמץ | קובץ |
|---|---|---|---|---|---|
| 1 | לרנדר את קישורי מחליף השפה תמיד | SEO | HIGH | **S** | `LanguageSwitcher.tsx:136` |
| 2 | לחבר את `CORPUS_REVISED` ולתקן את ה־`lastmod` של de/ja | SEO | HIGH | **S** | `blog/[slug]/page.tsx:12` |
| 3 | `line-height` יפני לכותרות | עיצוב | HIGH | **S** | `globals.css:2336` |
| 4 | להוסיף `hero-animate-delay-2` להגנת reduced-motion | נגישות | HIGH | **S** | `globals.css:2554` |
| 5 | להסתיר את ה־FAB כשה־CtaBand בתצוגה | מובייל | HIGH | **S** | `ScrollToTop.tsx` |
| 6 | למחוק `white-space: nowrap` מכותרת הטופס | עיצוב | HIGH | **S** | `FormClient.tsx:797` |
| 7 | מסנני הקטגוריה ל־`<Link>` | SEO | HIGH | **S** | `BlogClient.tsx:394` |
| 8 | `focusFirstError()` משותף לארבעת הטפסים | מובייל | HIGH | **M** | 4 טפסים |
| 9 | snapshot ב־debounce + `beforeunload` | מובייל | HIGH | **M** | `FormClient.tsx` |
| 10 | משוב התקדמות בהעלאה | מובייל | HIGH | **M** | `submit-tax-form.ts` |
| 11 | לשבור את שורת ההצהרה לשתי שורות | מובייל | HIGH | **M** | `FormClient.tsx:725` |
| 12 | לאחד את גרף הישויות ל־`@id` אחד ולהגדיר `#supervising-agent` | SEO | HIGH | **M** | 6 קבצים |
| 13 | `image` ל־`Article` + `isPartOf` לצומת הקנוני | SEO | MEDIUM | **S** | 3 שפות |
| 14 | למחוק את מעבר ה־premium הראשון | עיצוב | HIGH | **M** | `globals.css:2426` |
| 15 | לשקף ליפנית את 9 ההתאמות הגרמניות; למחוק 93 `letterSpacing` | עיצוב | HIGH | **L** | `src/app/ja/**` |
| 16 | root layouts לפי route group כדי לתקן `<html lang>` | SEO | HIGH | **L** | `layout.tsx` |

**שורות 1–7 הן S וכולן ניתנות ליישום בלי שיקול דעת עסקי.**

---

## מה כבר יושם הלילה

מתוך הרשימה הזו, שלושה פריטים בוצעו לפני שהדוח נכתב, כחלק מסבב התיקונים:

- **#14 בזרימה** — `formRoutes` הורחב, כך ש־`/tfn-form`, `/abn-form`,
  `/super-form` וגרסאות ה־de/ja שלהם כבר לא נושאים 22 קישורי בריחה בתחתית טופס.
- **#15 בזרימה** — שלושת מסנני ה־`onKeyDown` נמחקו והסינון עבר ל־`onChange`.
- **#16, #17 בזרימה** — `enterKeyHint` בכל ארבעת הטפסים, ו־`min`/`max` על שדות
  תאריך הלידה.

וכן, מהדוח על הקופי: הסתירה בין "under 10MB" ל־"under 4 MB" תוקנה בשלוש
השפות, ובאנר סיכום השגיאות כבר לא מדפיס שמות שדות באנגלית בתוך הטופס הגרמני
והיפני.
